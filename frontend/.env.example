# All optional. The deployed GIWA Sepolia addresses are baked into the app as fallbacks,
# so the hosted site works with no configuration. Override here only if you redeploy.
# VITE_API_URL=http://localhost:8899
# VITE_INVOICE_REGISTRY=
# VITE_PROOFPAY_ESCROW=
# VITE_DISPUTE_MODULE=
# VITE_DOJANG_VERIFIER=
